import CheckoutClient from './checkout-client';

export const metadata = {
  title: 'Checkout - Paperlisens',
  description: 'Selesaikan pesanan Anda di Paperlisens',
};

export default function CheckoutPage() {
  return <CheckoutClient />;
}
